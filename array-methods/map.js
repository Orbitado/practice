// Problema 3: Mapear a Cuadrados
// Dado un array de números, usa un método de array para crear un nuevo array que contenga el cuadrado de cada número.

const numeros = [1, 2, 3, 4, 5];

const result = numeros.map((numero) => numero * numero);

console.log(result);

// El método map() crea un nuevo array con los resultados de la llamada a la función indicada aplicados a cada uno de sus elementos.

// Itera cada elemento del arreglo y le aplica la función indicada.
