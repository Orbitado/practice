// Problema 20: Encontrar el Primer Número Mayor que 10
// Dado un array de números, encuentra y muestra el primer número que sea mayor que 10.

const numeros = [5, 8, 12, 3, 9, 15];

const result = numeros.find((numero) => numero > 10);
console.log(result);
