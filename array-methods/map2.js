// Problema 9: Crear un Array de Números Multiplicados por 10
// Dado un array de números, usa un método de array para crear un nuevo array donde cada número esté multiplicado por 10.

const numeros = [1, 2, 3, 4, 5];

const result = numeros.map((number) => number * 10);

console.log(result);
